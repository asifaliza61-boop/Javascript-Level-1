# Javascript-Level-1
1 Create a HTML & JS page.

Add a title and heading.

Add a <p> tag with an ID for output.

Add a button labeled “Click Me”.

Link the script.js file at the end of the body.

2 Use alert(), console.log(), and document.write() to show “Hello World”.

Create variables.

Make variables: name (string), age (number), isStudent (boolean).

Show them using console.log().

3 Do Basic Math

Add, subtract, multiply, divide two numbers.

Show results in the console.

4 Show Output in HTML

Use innerHTML to display values in the <p> tag.

5 Button Function

Create a function that shows an alert.

Link it to the button with onclick.
